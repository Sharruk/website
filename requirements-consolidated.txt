# College Materials & PYQs Portal - Consolidated Dependencies
# Compatible with Replit and local VS Code development

# Core Flask Framework
Flask>=3.1.1
Flask-SQLAlchemy>=3.1.1

# Web Server for Production
gunicorn>=23.0.0

# Database Support
SQLAlchemy>=2.0.41
psycopg2-binary>=2.9.10

# Security and File Handling
Werkzeug>=3.1.3
email-validator>=2.2.0

# Data Processing and Visualization
numpy>=2.2.6
matplotlib>=3.10.3
Pillow>=11.2.1

# AI Integration (Optional)
openai>=1.82.0

# Additional Utilities for File Processing
python-dotenv>=1.0.0
python-dateutil>=2.8.2
openpyxl>=3.1.2

# Installation Instructions:
# For Replit: Dependencies are managed automatically via pyproject.toml
# For Local Development:
#   1. Create virtual environment: python -m venv venv
#   2. Activate: source venv/bin/activate (Linux/Mac) or venv\Scripts\activate (Windows)
#   3. Install: pip install -r requirements-consolidated.txt
#   4. Run: python main.py

# Notes:
# - Uses minimum version specifiers (>=) for flexibility
# - All versions tested and compatible with Python 3.11+
# - PostgreSQL support included but currently using JSON storage
# - Supports drag-and-drop file uploads with all file types
# - Removes duplicates from project-requirements.txt and requirements-sample.txt
# - Updated to latest stable versions from pyproject.toml